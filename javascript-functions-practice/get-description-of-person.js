/* exported getDescriptionOfPerson */

function getDescriptionOfPerson(person) {
  return (person.name + ' is a ' + person.occupation + ' from ' + person.birthPlace + '.');
}

// var getDescriptionOfPersonResult = getDescriptionOfPerson('Ada Lovelace');
// console.log('getDescriptionOfPersonResult', getDescriptionOfPersonResult);
